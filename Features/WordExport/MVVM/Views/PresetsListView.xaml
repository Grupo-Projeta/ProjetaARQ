<UserControl x:Class="ProjetaARQ.Features.WordExport.MVVM.Views.PresetsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjetaARQ.Features.WordExport.MVVM.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewModel="clr-namespace:ProjetaARQ.Features.WordExport.MVVM.ViewModels"
             xmlns:converters="clr-namespace:ProjetaARQ.Core.UI.Converters"
             d:DataContext="{d:DesignInstance viewModel:PresetsListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ProjetaARQ;component/Features/WordExport/MVVM/Resources/DataTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!--<converters:BoolToVisibleConverter x:Key="BoolToVisible"/>-->
        </ResourceDictionary>
    </UserControl.Resources>
    
    
    <Border Background="Aquamarine">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
        
            <ListBox ItemsSource="{Binding Presets}"
                     SelectedItem="{Binding SelectedPreset, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Height="50">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding PresetName}" Margin="0 0 25 0"/>
                                <TextBlock Text="{Binding Rules.Count}" Margin="0 0 25 0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="1"
                    Command="{Binding EditPresetCommand}"
                    Style="{StaticResource MahApps.Styles.Button.Chromeless}"
                    Cursor="Hand"
                    Foreground="Black" FontSize="20">
                <iconPacks:PackIconMaterialDesign Kind="ModeEditOutlineOutline"/>
            </Button>
        </Grid>
    </Border>
</UserControl>
