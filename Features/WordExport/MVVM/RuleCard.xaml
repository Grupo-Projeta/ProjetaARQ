<UserControl x:Class="ProjetaARQ.Features.WordExport.MVVM.RuleCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="450"
             MinHeight="60" MaxHeight="500">

    <Expander IsExpanded="True" Style="{DynamicResource MahApps.Styles.Expander}">
        <Expander.Header>
            <Grid Width="400">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                        dd:DragDrop.IsDragSource="True"
                        Cursor="SizeAll"
                        ToolTip="Arrastar para reordenar">

                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </Button.Style>

                    <TextBlock Text="⠿" FontSize="18" Opacity="0.6" VerticalAlignment="Center" Margin="5,0"/>
                </Button>

                <TextBlock Grid.Column="1" Text="Nome da Regra (Ex: Substituir Cliente)" 
                           VerticalAlignment="Center" 
                           FontWeight="SemiBold" 
                           Margin="10,0"/>
            </Grid>
        </Expander.Header>

        <Border BorderBrush="LightGray" BorderThickness="1" Background="#FAFAFA" CornerRadius="3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20">
                    <TextBlock Text="Controles para configurar a regra..." HorizontalAlignment="Left"/>
                </Grid>

                <Thumb Grid.Row="1" Height="10" Cursor="SizeNS"
                       DragStarted="Thumb_DragStarted"
                       DragDelta="Thumb_DragDelta"
                       DragCompleted="Thumb_DragCompleted">
                    <Thumb.Style>
                        <Style TargetType="Thumb">
                            <Setter Property="Background" Value="LightGray"/>
                        </Style>
                    </Thumb.Style>
                </Thumb>
            </Grid>
        </Border>
    </Expander>
</UserControl>